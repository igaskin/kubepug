apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubepug.fullname" . }}
  labels:
    {{- include "kubepug.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "kubepug.selectorLabels" . | nindent 4 }}
